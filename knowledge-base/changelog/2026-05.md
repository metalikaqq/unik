# Changelog — 2026-05

## [2026-05-09] PHASE-01 complete — Design system shipped via Ralph loop

- **Author Type**: ai
- **AI Model**: claude-opus-4-7
- **Commit**: d260adb
- **Type**: feature

### Summary

Autonomous implementation of Phase 01 — Swiss-rigorous design system: tokens,
fonts, hooks, UI primitives, and `/dev/components` showcase with axe-clean E2E
baseline. Ralph loop closed all 8 stories (US-001 → US-008) across 8 iterations,
TDD-first throughout.

### Shipped

- **Tokens & fonts** (US-001 → US-003): CSS custom properties for color,
  typography scale, spacing, motion durations/easings; Inter Display + JetBrains
  Mono wired with `font-display: swap` and preload of critical weights.
- **Hooks** (US-002): `useReducedMotion`, `useEscapeKey`, `useScrollProgress` —
  all with unit tests.
- **Primitives** (US-004 → US-007): `Button`, `Tag`, `Rule`, `MarqueeText`,
  `Accordion` (compound, ARIA-correct, keyboard-nav), `Sheet` (focus-trapped,
  Esc/backdrop dismiss). `cn()` utility for class composition.
- **Showcase + E2E** (US-008): `/dev/components` page exercising every primitive;
  Playwright + axe-core baseline (`tests/e2e/design-system.spec.ts`) clean on
  chromium and firefox. Per-story E2E for Sheet (`tests/e2e/US-007.spec.ts`,
  5/5 pass).

### Impact

Every page in PHASE-02+ consumes these primitives — hierarchy, focus rings,
reduced-motion behavior, and a11y floor are now codified once and reused.
Future phases should not re-implement any of these surfaces; extend the tokens
or compose existing primitives instead.

### Related

- ADR-0002 Visual system: Swiss-rigorous brutalist
- ADR-0005 Tailwind v4 CSS-first config
- ADR-0006 Test stack: Vitest + Playwright + axe-core + Lighthouse CI
- Phase doc: `knowledge-base/phases/phase-01-design-system/`
- PHASE-OVERVIEW.md: PHASE-01 marked `completed`, exit criteria met

---

## [2026-05-08] Phase 01 Kickoff — Design System + Tokens

- **Author Type**: ai
- **AI Model**: claude-opus-4-7
- **Type**: milestone

### Started

- PHASE-01: Design System + Tokens + UI Primitives — codify tokens, build the
  6 primitives every page consumes, establish a11y floor (focus rings, traps,
  reduced motion). 17 tasks; ~3 working days; critical path 1.1→1.4→1.6→(1.13‖1.14)→1.15→1.16.

### Transitions

- PLAN.md status `proposed` → `planned` (content was already complete).
- SPEC.md status `proposed` → `active`; `started: 2026-05-08`.
- STATUS.md generated with task / deliverable / exit-criteria trackers.

### Dependencies

- PHASE-00 (bootstrap): `completed` — gate passed 2026-05-08, no blockers.

### Notes

- No `wiki/` directory in repo, so wiki step skipped.
- Next.js 16 — per `AGENTS.md`, all primitive code must consult
  `node_modules/next/dist/docs/` before relying on training-data assumptions.

---

## [2026-05-08] PHASE-00 GATE: PASS — bootstrap shipped to Vercel

- **Author Type**: human + ai
- **AI Model**: claude-opus-4-7
- **Type**: milestone

### Shipped

- All 5 PHASE-00 deliverables verified.
- 6 commits on `main` (1 docs, 1 chore, 1 test, 1 ci, 1 docs/status, 1 docs/gate).
- Repo pushed to `metalikaqq/unik` on GitHub (remote rewritten to use the
  `github.com-personal` SSH alias).
- Vercel project `metalikaqqs-projects/unik` deployed to canonical alias
  https://unik-bice.vercel.app/.
- Lighthouse 100/100/100/100 across 2 desktop runs against the canonical alias.
- Playwright + axe-core sanity 2/2 against the same URL.

### Gate evidence

See `knowledge-base/phases/phase-00-bootstrap/GATE.md`.

### PLAN.md amendment

- **0.3a (new)** — Install dev deps + configure pnpm `onlyBuiltDependencies`
  allowlist. Documented in `STATUS.md → PLAN.md Amendments`.

### Side effects (recorded in GATE.md)

1. Vercel SSO Protection toggled off via API to make the canonical alias
   publicly reachable for Lighthouse/Playwright.
2. Git remote rewritten from default-host SSH alias to `github.com-personal`
   to match the `metalikaqq` GitHub identity.
3. Vercel ↔ GitHub auto-deploys not yet wired (one-click dashboard step
   deferred to PHASE-09 prep).

### Status

PHASE-00: `completed`. PHASE-01 (Design system + tokens) eligible for kickoff.

---

## [2026-05-08] Project planning complete — 11 phases proposed

- **Author Type**: human + ai
- **AI Model**: claude-opus-4-7
- **Type**: milestone

### Created

- 6 ADRs in `knowledge-base/decisions/`:
  - ADR-0001 Stack: Next.js 16 App Router + TypeScript strict
  - ADR-0002 Visual system: Swiss-rigorous brutalist
  - ADR-0003 Bilingual UA / EN with next-intl, UA canonical
  - ADR-0004 No backend; Formspree free tier for CFP
  - ADR-0005 Tailwind v4 CSS-first config
  - ADR-0006 Test stack: Vitest + Playwright + axe-core + Lighthouse CI
- 11 phase SPEC.md + PLAN.md pairs in `knowledge-base/phases/phase-{00..10}-*/`
- `knowledge-base/phases/PHASE-OVERVIEW.md` index with status board, dependency graph, cross-cutting test strategy, project-level risks, and chapter mapping

### Decisions locked

- Conference identity: **CARPATHIAN.CONF 2026** in Uzhhorod, 14–16 May 2026
- Visual: B/W + `#E63946` Carpathian red accent; Inter Display + JetBrains Mono
- Pages (5): Home / Speakers / Schedule / Tickets / Venue
- Total estimate: ~30 working days (sits inside the March–May practical window)

### Pre-planning state

- Empty git repo `metalikaqq/unik` was scaffolded with `create-next-app` (Next.js 16, TS, Tailwind v4) and runtime/dev deps installed before planning. That work is captured under PHASE-00 task 0.1–0.2 — to be re-verified during phase kickoff.

### Status

All 11 phases are at status `proposed`. Awaiting user approval to transition PHASE-00 to `planned` → `active` and begin implementation.
